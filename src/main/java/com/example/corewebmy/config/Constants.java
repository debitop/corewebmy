package com.example.corewebmy.config;

public interface Constants {
    String USERNAME_PASSWORD = "^{A-Za-z0-9}";
    String USER_ALREADY_EXIST = "user already exist";
    String USER_ALREADY_EXIST_DESCRIPTION = "user already exist";
}
